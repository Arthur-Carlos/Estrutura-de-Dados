#include <stdio.h>
#define MAX 100000

int main()
{
    int entrada, i;
    char palavra[MAX][11];
    scanf("%d", &entrada);
    for (i = 0; i < entrada; i++)
    {
        scanf("%s", palavra[i]);
    }
    for (i = 0; i < entrada; i++)
    {
        printf("%s\n", palavra[i]);
    }
    return 0;
}
